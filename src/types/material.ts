// --- Used in DailyLogPage (PM) ---
export interface MaterialResponse {
  id: number
  daily_log_id: number
  name: string
  quantity: number
  unit: string
  unit_cost: number
  total_cost: number
}